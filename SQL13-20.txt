14.

Une vue permet de représenter la BD sous une forme plus proche de la vision qu'en a l'utilisateur.
Finalement, il s'agit d'un ensemble de tables liées entre-elles (jointures par exemple) pour rendre la visualisation de l'information plus globale.

Mis à jour des données rarement possible ( contrairement aux tables ).


15.

SELECT NUMERO_SEANCE, NUMERO_PLACE, NOM_SPECTATEUR 
FROM RESERVATION NATURAL JOIN PLACE WHERE PLACE.CATEGORIE_DE_LA_PLACE='A';


(??)


16.

SELECT VIEW_NAME from ALL_VIEWS where OWNER='ADMIN';



17.


select TEXT
FROM ALL_VIEWS 
WHERE OWNER= 'ADMIN' and VIEW_NAME='RESERVATION_CATEGORIE_A';



18.

SELECT NUMERO_SEANCE, NUMERO_PLACE, NOM_SPECTATEUR 
FROM RESERVATION_CATEGORIE_A;



19.

SELECT NUMERO_SEANCE, NUMERO_PLACE, NOM_SPECTATEUR 
FROM RESERVATION_CATEGORIE_A NATURAL JOIN SEANCE WHERE NUMERO_FILM=19
ORDER BY NOM_SPECTATEUR;


20.

SELECT NUMERO_SEANCE, COUNT(*)
FROM RESERVATION_CATEGORIE_A
GROUP BY NUMERO_SEANCE
ORDER BY NUMERO_SEANCE;
